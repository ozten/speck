name: plan-integration-test
recorded_at: '2026-01-20T14:00:00Z'
commit: abc123test
interactions:
# --- Pass 0: Document scoring ---
# 1. llm.complete — score document for specificity and verifiability
- seq: 0
  port: llm
  method: complete
  input: {}
  output:
    ok:
      text: '{"specificity_score": 75, "specificity_issues": ["Missing edge cases for invalid input"], "verifiability_score": 70, "verifiability_issues": ["No explicit acceptance criteria"], "questions": [{"description": "How should auth failures be reported?", "options": ["Return HTTP 401 with error body (recommended)", "Redirect to login page"], "recommended": 0}], "recommendations": ["Add: authentication failures return HTTP 401 with JSON error body"]}'
      prompt_tokens: 400
      completion_tokens: 100
# --- Pass 1: Broad survey ---
# 2. git.current_commit for cache check
- seq: 0
  port: git
  method: current_commit
  input: {}
  output: abc123test
# 2. fs.exists — cache miss
- seq: 1
  port: fs
  method: exists
  input: {}
  output: false
# --- Map generation (no cache) ---
# 3. clock.now
- seq: 2
  port: clock
  method: now
  input: {}
  output: '2026-01-20T14:00:00Z'
# 4. git.current_commit (called again by generator)
- seq: 3
  port: git
  method: current_commit
  input: {}
  output: abc123test
# 5. git.list_files
- seq: 4
  port: git
  method: list_files
  input: {}
  output:
  - src/lib.rs
  - src/main.rs
# 6. fs.read_to_string — src/lib.rs
- seq: 5
  port: fs
  method: read_to_string
  input: {}
  output: "pub fn run() {}\n"
# 7. fs.read_to_string — src/main.rs
- seq: 6
  port: fs
  method: read_to_string
  input: {}
  output: "fn main() { run(); }\n"
# 8. fs.write — save cached codebase map
- seq: 7
  port: fs
  method: write
  input: {}
  output: null
# 9. llm.complete — survey analysis
- seq: 8
  port: llm
  method: complete
  input: {}
  output:
    ok:
      text: '{"routing_table": {"src": "Application entry point and core logic"}, "cross_cutting_concerns": ["error handling"], "foundational_gaps": []}'
      prompt_tokens: 500
      completion_tokens: 100
# --- Pass 2: Signal classification ---
# 10. llm.complete — classification
- seq: 9
  port: llm
  method: complete
  input: {}
  output:
    ok:
      text: '{"type": "clear", "checks": [{"check_type": "command_output", "command": "cargo run -- add-auth", "expected": "CLI outputs expected message"}, {"check_type": "command_output", "command": "cargo run -- add-auth; echo $?", "expected": "0"}]}'
      prompt_tokens: 300
      completion_tokens: 80
# --- Pass 3: Conversation loop ---
# 11. llm.complete — analysis (all resolved, empty questions)
- seq: 10
  port: llm
  method: complete
  input: {}
  output:
    ok:
      text: '{"summary": "All task specs have clear verification strategies.", "questions": []}'
      prompt_tokens: 200
      completion_tokens: 50
# --- Pass 2.5: Reconciliation ---
# 12. llm.complete — reconciliation
- seq: 11
  port: llm
  method: complete
  input: {}
  output:
    ok:
      text: '{"merges": [], "extractions": [], "reorders": []}'
      prompt_tokens: 300
      completion_tokens: 50
# --- Spec persistence ---
# 13. id_gen.generate_id — assign spec ID
- seq: 12
  port: id_gen
  method: generate_id
  input: {}
  output: TASK-PLAN-1
# 14. fs.write — save task spec to store
- seq: 13
  port: fs
  method: write
  input: {}
  output: null
